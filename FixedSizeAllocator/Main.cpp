#include "BitArray.h"
#include "FixedSizeAllocator.h"
#include <iostream>
using namespace std;

int main()
{

	//BitArray* tmp = BitArray::Create(100,true,malloc(sizeof()))
	//tmp->TEST();
	return 0;
}